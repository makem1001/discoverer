# 发现者（Discoverer）— 前端生产 Dockerfile
# 使用预构建的 dist，不执行 npm build（需先在本机 npm run build）
FROM nginx:alpine
COPY dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
